import { type Font } from "../core/font.js";
import { View } from "./view.js";
export declare class Textarea extends View {
    font: Font;
    color: number;
    private label;
    private _cursor;
    canFocus: boolean;
    canMouse: boolean;
    private lines;
    multiline: boolean;
    onEnter?(): void;
    get text(): string;
    set text(s: string);
    highlightings: Record<string, [number, RegExp]>;
    cursorColor: number;
    private row;
    private col;
    private end;
    private colors;
    xgap: number;
    ygap: number;
    highlight(): void;
    init(): void;
    onMouseDown(button: number): void;
    private drawTextLabel;
    private adjustTextLabel;
    private reflectCursorPos;
    private findScrollAncestor;
    private scrollCursorIntoView;
    onKeyDown(key: string): boolean;
    private halves;
    private fixCol;
    private blink?;
    private restartBlinking;
    private stopBlinking;
    onFocus(): void;
    onBlur(): void;
}
